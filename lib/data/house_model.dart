import 'package:house_generator/domain/house.dart';

class HouseModel extends House {
  HouseModel(
      {required super.name,
      required super.region,
      required super.attributes,
      required super.isLandedHouse});
}
